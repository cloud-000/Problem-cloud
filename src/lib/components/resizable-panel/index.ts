import Root from "./resizable-panel.svelte";

export { Root, Root as ResizablePanel };
export type { ResizablePanelProps } from "./resizable-panel.svelte";
export type {
    CollapsibleDimensions,
    PanelSize,
    PanelSizeConstraints,
    ResizeEdge,
} from "./resize";
