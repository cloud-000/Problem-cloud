import Root, { type ToastProps, toastVariants } from "./toast.svelte";
import Container, {
    type ToastContainerProps,
} from "./toast-container.svelte";
export {
    Root,
    Container,
    //
    Root as Toast,
    Container as ToastContainer,
    toastVariants,
    type ToastProps,
    type ToastProps as Props,
    type ToastContainerProps,
};
