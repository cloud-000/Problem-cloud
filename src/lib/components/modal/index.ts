import Root, {
    type ModalProps,
    type ModalSize,
} from "./modal.svelte";
import ModalContainer from "./modal-container.svelte";

export {
    Root,
    Root as Modal,
    ModalContainer,
    type ModalProps,
    type ModalSize,
};
