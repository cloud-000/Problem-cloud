import Root, {
    type ModalProps,
    type ModalSize,
} from "./modal.svelte";
import ModalContainer from "./modal-container.svelte";
import ConfirmModal from "./ConfirmModal.svelte";

export {
    Root,
    Root as Modal,
    ModalContainer,
    ConfirmModal,
    type ModalProps,
    type ModalSize,
};
